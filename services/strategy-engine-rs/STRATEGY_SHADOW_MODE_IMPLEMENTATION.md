# Strategy Shadow Mode Implementation

Shadow mode runs candidate strategies alongside production counterparts.
- Uses `rust_decimal` for accuracy.
- Clamped relative percentage differences.
- Computes aggregated stats and match rates.
