# Position Domain

Tracks position states and bounds safely.

## Structures
- **Position**: Stores symbol, direction, entry_price, size, stop_loss, and take_profit using `rust_decimal::Decimal`.
- **PositionState**: Opening, Open, Closing, Closed
