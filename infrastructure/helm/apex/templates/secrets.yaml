apiVersion: v1
kind: Secret
metadata:
  name: apex-secrets
type: Opaque
stringData:
  databaseUrl: {{ .Values.global.databaseUrl | quote }}
  # Placeholder for external secrets sync or vault integration
  # Other secrets like GEMINI_API_KEY, GROQ_API_KEY can be added here
